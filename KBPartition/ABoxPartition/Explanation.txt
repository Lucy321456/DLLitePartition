1. compute individual index, i.e., for an individual Ind, compute the number of assertions using Ind as individual.
2. according to individual index, partition individuals into multiple groups;
3. for each individual group G, compute a subABox consisting of all the assertions containing individuals in G.